# State Management Store

Set up Redux or Zustand here for global state management.

Example structure:

```
store/
├── slices/
│   ├── chatSlice.ts
│   └── voiceSlice.ts
├── store.ts
└── hooks.ts  # useAppDispatch, useAppSelector, etc.
```
